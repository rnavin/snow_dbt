customer_pipeline:
  target : dev 
  outputs:
    dev:
      type: snowflake
      account: ****************
      user: rnavin
      password:*************
      role: ACCOUNTADMIN
      databse: test_dw
      warehouse : COMPUTE_WH
      schema : public
      authenticator: snowflake
